{% 
  set list = [
    { name: 'United States', count: 147 },
    { name: 'Europe', count: 21 },
    { name: 'United Kingdom', count: 54 },
    { name: 'Asia & Pacific', count: 73 },
    { name: 'Africa', count: 39 },
    { name: 'Latin America', count: 62 }
  ]
%}
<div class="bg-grey-50" id="about">
  <div class="container flex flex-col items-top py-6 md:py-8 lg:flex-row">
    <div class="w-full text-center hidden md:block lg:block md:w-3/4 lg:w-3/5 lg:text-left">
      <h2
        class="font-header text-3xl font-semibold uppercase text-primary sm:text-3xl lg:text-4xl"
      >
        Our Mission
      </h2>
      <h4
        class="pt-2 font-header text-xl font-medium text-black sm:text-xl lg:text-2xl"
      >
        We deliver the carefuly curated list of neo banks around the globe
      </h4>
      <p class="pt-4 font-body leading-relaxed text-grey-20">
        Having neobanking rapidly emerging all around the
        world, we define our mission as delivering the accurate
        list of neobanks with the most important details
        about them
      </p>
      {#% include './_social.njk' %#}
    </div>
    <div class="w-full text-center text-xl hidden text-primary smx:block uppercase">
      <p class="pt-2 pb-4 font-header font-medium text-black text-xl leading-relaxed">
        We deliver the carefuly curated list of neo banks around the globe
      </p>
      
    </div>
    <div class="w-full pl-0 sm:w-full lg:w-3/5 lg:pl-12">
      <div>
        <h2 class="font-header text-3xl uppercase text-primary px-4 mb-4">
          Banks in the system
        </h2>
        <div class="flex items-top lg:flex-row flex-wrap">

          {% for item in list %}
          <div class="w-1/2 px-4">
            <div class="flex items-end justify-between">
              <h4 class="font-body font-semibold uppercase text-black">
                {{ item.name }}
              </h4>
              <h3 class="font-body text-2xl font-bold text-primary">{{item.count}}</h3>
            </div>
          </div>  
          {% endfor %}
          
        </div>
      </div>
    </div>
  </div>
</div>
